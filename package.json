{
  "name": "AppChat",
  "private": true,
  "scripts": {
    "start": "meteor run --port 2000",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.3.4",
    "bcrypt": "^3.0.6",
    "meteor-node-stubs": "^0.4.1"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
